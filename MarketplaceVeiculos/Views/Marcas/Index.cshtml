@model IEnumerable<MarketplaceVeiculos.Models.Marca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_marca)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome_marca)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_marca">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_marca">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_marca">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
